<h1>Tags</h1>

<%= link_to 'Add Tag', new_tag_path %>

<%# @books.each do |book| %>
  <%#= book.title %>
  <%# book.taggings.each do |t| %>
    <%#= t.tag.title %>
  <%# end %>
<%# end %>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.title, tag_path(tag) %>
      
    </li>
  <% end %>
</ul>