<h1>Tags</h1>

<%= link_to 'Add Tag', new_tag_path %>

<ul>
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.title, tag %>
      <%= link_to 'Edit', edit_tag_path(tag) %>
      <%= link_to 'Delete', tag, method: :delete, data: { confirm: 'Sure?' } %>
    </li>
  <% end %>
</ul>